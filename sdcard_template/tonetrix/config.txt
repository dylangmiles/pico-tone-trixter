# Tone Trixter — global config
# Flow-style YAML: flat "key: value", # comments. No significant indentation.

boot_preset: default    # preset loaded at power-on (name from presets.txt)
gr_meter: off           # home-screen gain-reduction bar: on / off
# ES8388 input PGA gain is per-PRESET (see presets.txt `pga:`) — but as of 2026-07-24 every
# preset runs pga 0 and should stay there: the ADC clips at 1.35 V pk, and gain ahead of it
# buys no SNR (+18 dB pga measured +17.4 dB more noise). Make up level digitally instead.
# Still settable live via UART 'pga' / the menu for A/B and measurement. No global PGA key.
