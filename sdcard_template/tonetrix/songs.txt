# Tone Trixter — songs (SONG MODE)
# =========================================================================
# Flow-style YAML, same shape as presets.txt: "name:" starts a song, "---"
# between songs is optional, # comments, no significant indentation.
#
#   name:      shown on screen in large type — keep it short (~10 chars)
#   preset:    a preset name from presets.txt; omit to keep whatever is loaded
#   backing:   a file in /tonetrix/backing (with or without .wav), or none
#
# On the pedal (Daisy build): HOLD the tuner footswitch to enter song mode.
#   bypass switch  tap  = start / stop the backing      hold = bypass toggle
#   tuner  switch  tap  = next song                     hold = leave song mode
#   encoder        turn = previous / next song
# Picking a song loads its preset and engages the chain; it does not start
# the backing until you tap — so arm the song, then start it on the beat.
#
# Later a song may be a looping session with a bpm and sounds triggered from
# the switches; for now it is these three keys.

name: Blues 71
preset: tanglewood-slide
backing: blues71
---
name: Folk 104
preset: default
backing: folk104
---
name: Solo
preset: garrison
backing: none
